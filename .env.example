# Bundle Kit — local development and Vercel production
# 1. Copy to .env: cp .env.example .env
# 2. Fill in values below.
#
# --- Database (Supabase) ---
# Get DB password: https://supabase.com/dashboard/project/ckmjeytchwuztartbiyx/settings/database
# DATABASE_URL: Transaction mode (6543) for serverless/Vercel. Add ?pgbouncer=true
#   (Supavisor doesn't support prepared statements; Prisma needs this to disable them).
# DIRECT_URL: Session mode (5432) for Prisma Migrate only.
DATABASE_URL="postgresql://postgres.ckmjeytchwuztartbiyx:[YOUR-PASSWORD]@aws-1-us-east-1.pooler.supabase.com:6543/postgres?pgbouncer=true"
DIRECT_URL="postgresql://postgres.ckmjeytchwuztartbiyx:[YOUR-PASSWORD]@aws-1-us-east-1.pooler.supabase.com:5432/postgres"
#
# --- Shopify App (from `shopify app env show`) ---
SHOPIFY_API_KEY=
SHOPIFY_API_SECRET=
SCOPES=write_products,read_discounts,write_discounts
# Production: set to your Vercel URL after first deploy (e.g. https://bundle-kit.vercel.app)
SHOPIFY_APP_URL=http://localhost:3000
#
# --- Optional ---
# NODE_ENV=production
# SHOPIFY_BUNDLE_DISCOUNT_ID=  # Set after deploying bundle-discount extension
